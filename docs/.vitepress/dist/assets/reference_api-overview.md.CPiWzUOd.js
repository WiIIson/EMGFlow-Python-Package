import{_ as t,c as o,o as i,ag as a}from"./chunks/framework.DPDPlp3K.js";const u=JSON.parse('{"title":"API Overview","description":"","frontmatter":{"outline":"deep"},"headers":[],"relativePath":"reference/api-overview.md","filePath":"reference/api-overview.md","lastUpdated":null}'),l={name:"reference/api-overview.md"};function s(n,e,r,d,c,h){return i(),o("div",null,e[0]||(e[0]=[a('<h1 id="api-overview" tabindex="-1">API Overview <a class="header-anchor" href="#api-overview" aria-label="Permalink to &quot;API Overview&quot;">​</a></h1><p>EMGFlow is broken into 5 models - FileIO, preprocessing signals, feature extraction, detecting outliers, and generating visualizations.</p><h2 id="modules" tabindex="-1">Modules <a class="header-anchor" href="#modules" aria-label="Permalink to &quot;Modules&quot;">​</a></h2><h3 id="fileaccess" tabindex="-1"><code>FileAccess</code> <a class="header-anchor" href="#fileaccess" aria-label="Permalink to &quot;`FileAccess`&quot;">​</a></h3><p>These functions provide helper methods for accessing files, and are mostly used internally by the package.</p><p>One notable function, <code>MapFiles</code>, takes a path to a folder, and generates a dictionary of paths to files contained within. This makes it easier to create a loop over subfiles, reading them in and performing analysis.</p><p><code>MapFiles</code> forms the basis for the two modes of analysis offered by EMGFlow - automated, or manual.</p><p>The &quot;automated&quot; mode makes the processing pipeline much simpler. In these functions, a file location is provided, default parameters are set, and a file output location is set. The functions then apply the filters to each file found in the folder, and output the filtered files to the output folder. Notable functions include:</p><ul><li><code>NotchFilterSignals</code></li><li><code>BandpassFilterSignals</code></li><li><code>SmoothFilterSignals</code></li></ul><p>The &quot;manual&quot; mode allows for additional customization of processing. In these functions, a dataframe is provided, filter parameters are set, and the dataframe is returned. These functions are useful if the processing pipeline requires further additional processing before being outputted, of if the project is not large-scale enough to warrent batch processing. Notable functions include:</p><ul><li><code>ApplyNotchFilters</code></li><li><code>ApplyBandpassFilter</code></li><li><code>ApplyRMSSmooth</code></li></ul><p>For more information about file accessing functions, see <a href="./file-access.html">FileAccess API</a>.</p><h3 id="preprocesssignals" tabindex="-1"><code>PreprocessSignals</code> <a class="header-anchor" href="#preprocesssignals" aria-label="Permalink to &quot;`PreprocessSignals`&quot;">​</a></h3><p>This module provides preprocessing functions for cleaning sEMG signals prior to their use in feature extraction. Signal processing is broken into 3 parts: notch filtering, bandpass filtering and smoothing. Each part has additional functions that support more specific needs, explained in more detail in the module descriptions.</p><h4 id="notchfiltersignals" tabindex="-1"><code>NotchFilterSignals()</code> <a class="header-anchor" href="#notchfiltersignals" aria-label="Permalink to &quot;`NotchFilterSignals()`&quot;">​</a></h4><h5 id="description" tabindex="-1">Description <a class="header-anchor" href="#description" aria-label="Permalink to &quot;Description&quot;">​</a></h5><p>Notch filtering involves filtering specific frequencies. This is typically due to some sort of interference, such as the power source of the device taking the reading.</p><h5 id="parameters" tabindex="-1">Parameters <a class="header-anchor" href="#parameters" aria-label="Permalink to &quot;Parameters&quot;">​</a></h5><p>Notch filtering is controlled by the function <code>NotchFilterSignals()</code>, with the following main parameters:</p><ul><li><code>in_path</code> &lt;-- File path to input folder</li><li><code>out_path</code> &lt;-- File path to output folder</li><li><code>sampling_rate</code> &lt;-- Sampling rate of the data</li><li><code>notch</code> &lt;-- Notch filters to be applied</li></ul><p><code>in_path</code> refers to the folder containing the signal data. The function will be able to detect signals in the folder, and in any subfolders.</p><p><code>out_path</code> refers to the folder where the filtered signals will be output to. The function will automatically recreate the file structure of the input folder with additional subfolders.</p><p><code>sampling_rate</code> refers to the sampling rate of the data. The function will assume that each signal, and each column, is using the same sampling rate.</p><p><code>notch</code> refers to the notch filters being applied to the signal. This takes the form of a list of <code>(Hz,Q)</code> tuples. <code>Hz</code> is the frequency to which the filter is being applied, and <code>Q</code> is the Q-factor (strength of the filter, where a lower number is a stronger filter). The Q-factor needs to be explored to find a suitable value. When the Q-factor is too high, there will be barely any effect. When the Q_factor is too, important frequencies around the target being filtered out. All filters in the list will be applied to each column and signal.</p><h5 id="value-added" tabindex="-1">Value-Added <a class="header-anchor" href="#value-added" aria-label="Permalink to &quot;Value-Added&quot;">​</a></h5><p><code>NotchFilterSignals()</code> provides flexibility for use in different regions of the world. Some filtering packages only provide notch filtering for 60Hz, the frequency where power can interfere with signal readings. However, other regions use 50Hz frequencies.</p><p>For more information about further customizations and detail about <code>NotchFilterSignals()</code>, see <a href="./preprocess-signals.html">PreprocessSignals API</a>.</p><h4 id="bandpassfiltersignals" tabindex="-1"><code>BandpassFilterSignals()</code> <a class="header-anchor" href="#bandpassfiltersignals" aria-label="Permalink to &quot;`BandpassFilterSignals()`&quot;">​</a></h4><p><strong>Description</strong></p><p>Bandpass filtering involves specifying a range of frequencies to keep, and removing all other frequencies outside this range. This is useful to remove interference outside the most meaningful range of readings.</p><p><strong>Parameters</strong></p><p>Bandpass filtering is controlled by the function <code>BandpassFilterSignals()</code>, with the following parameters:</p><ul><li><code>in_path</code> &lt;-- File path to input folder</li><li><code>out_path</code> &lt;-- File path to output folder</li><li><code>sampling_rate</code> &lt;-- Sampling rate of the data</li><li><code>low</code> &lt;-- Lower frequency range</li><li><code>high</code> &lt;-- Upper frequency range</li></ul><p><code>in_path</code> refers to the folder containing the signal data. The function will be able to detect signals in the folder, and in any subfolders.</p><p><code>out_path</code> refers to the folder where the filtered signals will be output to. The function will automatically recreate the file structure of the input folder with additional subfolders.</p><p><code>sampling_rate</code> refers to the sampling rate of the data. The function will assume that each signal, and each column, is using the same sampling rate.</p><p><code>low</code> and <code>high</code> are the thresholds for the bandpass filter. All frequencies outside the range provided are filtered out of the signal. The defaults respectively are 20 and 450.</p><p><strong>Value-Added</strong></p><p><code>BandpassFilterSignals()</code> uses bandpass thresholds of 20Hz and 450Hz, as this is default for EMG signals (De Luca et al., 2010). However, there is some disagreement within literature for different muscels, so <code>BandpassFilterSignals()</code> provides the option to change the thresholds.</p><p>For more information about further customizations and detail about <code>BandpassFilterSignals()</code>, see <a href="./preprocess-signals.html">PreprocessSignals API</a>.</p><h3 id="smoothfiltersignals" tabindex="-1"><code>SmoothFilterSignals()</code> <a class="header-anchor" href="#smoothfiltersignals" aria-label="Permalink to &quot;`SmoothFilterSignals()`&quot;">​</a></h3><p>Smoothing involves limiting the impacts of noise and outliers in the signal. By default, this function uses the RMS smoothing method, as it is the best choice for smoothing EMG signals (RENSHAW et al., 2010).</p><p>Smoothing is controlled by the function <code>SmoothFilterSignals()</code>, with the following main parameters:</p><ul><li><code>in_path</code> &lt;-- File path to input folder</li><li><code>out_path</code> &lt;-- File path to output folder</li><li><code>sampling_rate</code> &lt;-- Sampling rate of the data</li><li><code>window_size</code> &lt;-- Size of the window to smooth</li><li><code>method</code> &lt;-- Smoothing method to use</li></ul><p><code>in_path</code> refers to the folder containing the signal data. The function will be able to detect signals in the folder, and in any subfolders.</p><p><code>out_path</code> refers to the folder where the filtered signals will be output to. The function will automatically recreate the file structure of the input folder with additional subfolders.</p><p><code>sampling_rate</code> refers to the sampling rate of the data. The function will assume that each signal, and each column, is using the same sampling rate.</p><p><code>window_size</code> refers to the size of the window when the data is being smoothed. This affects how large of an area around the value to take an average of.</p><p><code>method</code> refers to the smoothing method to use. Can be one of &#39;rms&#39;, &#39;boxcar&#39;, &#39;gauss&#39; or &#39;loess&#39;. The default is &#39;rms&#39;.</p><p><strong>Value-Added</strong></p><p><code>SmoothFilterSignals()</code> by default uses the RMS smoothing method, as it is the best choice for filtering EMG signals (RENSHAW et al., 2010). Regardless, EMGFlow provides different methods for smoothing signals which can be used instead.</p><p>For more information about further customizations and detail about <code>SmoothFilterSignals()</code>, see <a href="./preprocess-signals.html">PreprocessSignals API</a>.</p><hr><h2 id="extractfeatures-module" tabindex="-1"><code>ExtractFeatures</code> Module <a class="header-anchor" href="#extractfeatures-module" aria-label="Permalink to &quot;`ExtractFeatures` Module&quot;">​</a></h2><p>This module takes preprocessed data, and extracts features from the sEMG signal that capture information in both time and frequency domains. The main function to do this is <code>ExtractFeatures</code>. Within this call, individual features are calculated by their own functions, allowing them to be incorporated into your own workflow.</p><h3 id="extractfeatures" tabindex="-1"><code>ExtractFeatures()</code> <a class="header-anchor" href="#extractfeatures" aria-label="Permalink to &quot;`ExtractFeatures()`&quot;">​</a></h3><p><strong>Description</strong></p><p>Analysis involves extracting the features from each signal into a feature file. This is the end of the pipeline, producing the final result.</p><p>Analysis is controlled by the function <code>AnalyzeSignals()</code>, with the following main parameters:</p><ul><li><code>in_bandpass</code> &lt;-- File path to input folder of data before being smoothed</li><li><code>in_smooth</code> &lt;-- File path to input folder of data after being smoothed</li><li><code>out_path</code> &lt;-- File path to output folder</li><li><code>sampling_rate</code> &lt;-- Sampling rate of the data</li></ul><p><code>AnalyzeSignals()</code> extracts the following time-series features:</p><ul><li>Min</li><li>Max</li><li>Mean</li><li>SD</li><li>Skew</li><li>Kurtosis</li><li>IEMG</li><li>MAV</li><li>MMAV</li><li>SSI</li><li>VAR</li><li>VOrder</li><li>RMS</li><li>WL</li><li>LOG</li><li>MFL</li><li>AP</li><li>Spectral Flux</li></ul><p>And the following spectral features:</p><ul><li>Max frequency</li><li>MNF</li><li>MDF</li><li>Twitch ratio</li><li>Twitch index</li><li>Twitch slope fast</li><li>Twitch slope slow</li><li>Spectral centroid</li><li>Spectral flatness</li><li>Spectral decrease</li><li>Spectral entropy</li><li>Spectral rolloff</li><li>Spectral bandwidth</li></ul><p>This function requires a path to smoothed and unsmoothed data. This is because while time-series features are extracted from smoothed data, spectral features are not. High-frequency components of the signal can be lost in the smoothing, and we want to ensure the spectral features are as accurate as possible.</p><p>For more information about further customizations and specifications that can be made to <code>AnalyzeSignals()</code>, see <a href="./preprocess-signals.html">PreprocessSignals API</a>.</p><p>For a more detailed explanation about the features extracted by <code>AnalyzeSignals()</code>, see <a href="./feature-extraction.html">ExtractFeatures API</a>.</p><hr><h2 id="outlierfinder-module" tabindex="-1"><code>OutlierFinder</code> Module <a class="header-anchor" href="#outlierfinder-module" aria-label="Permalink to &quot;`OutlierFinder` Module&quot;">​</a></h2><p>This module provides methods to help detect signal files that contain outliers. This helps for workflows involving batch processing of files, where it might be harder to determine if there are any patterns, or specific files that need additional filters applied.</p><p>Outlier detection is handled by the function <code>DetectOutliers()</code>. This function fits an inverse graph to the PSD representation of the signal, and identifies if there is a value significantly above a threshold.</p><ul><li><code>in_path</code> &lt;-- File path to input folder</li><li><code>sampling_rate</code> &lt;-- Sampling rate of the data</li><li><code>threshold</code> &lt;-- Threshold to consider a value an outlier</li></ul><p><code>in_path</code> refers to the folder containing the raw signal data. The function will be able to detect signals in the folder, and in any subfolders.</p><p><code>sampling_rate</code> refers to the sampling rate of the data. The function will assume that each signal, and each column, is using the same sampling rate.</p><p><code>threshold</code> refers to the number of times above the median for a value to be considered an outlier. This is checked for each column in the signal file, and is marked as an outlier if an outlier is found in any column.</p><p>This function outputs a dictionary of file names and locations for each signal marked as an outlier.</p><p>For more information about further customizations and specifications that can be made to <code>DetectOutliers()</code>, see <a href="./outlier-detection.html">OutlierFinder API</a>.</p><hr><h2 id="plotsignals-module" tabindex="-1"><code>PlotSignals</code> Module <a class="header-anchor" href="#plotsignals-module" aria-label="Permalink to &quot;`PlotSignals` Module&quot;">​</a></h2><p>The plotting module <code>PlotSignals</code> provides functions to help visualize individual, or large batches of signal data. This helps visually see what is happening in a signal to identify outliers, and determine the kinds of filters that need to be applied.</p><h3 id="plotspectrum" tabindex="-1"><code>PlotSpectrum()</code> <a class="header-anchor" href="#plotspectrum" aria-label="Permalink to &quot;`PlotSpectrum()`&quot;">​</a></h3><p><code>PlotSpectrum</code> takes care of generating large numbers of signal plots for all signals contained in a folder and subfolders.</p><ul><li><code>in_path</code> &lt;-- File path to input folder</li><li><code>out_path</code> &lt;-- File path to output folder</li><li><code>sampling_rate</code> &lt;-- Sampling rate of the data</li></ul><p><code>in_path</code> refers to the folder containing the raw signal data. The function will be able to detect signals in the folder, and in any subfolders.</p><p><code>out_path</code> refers to the folder where the plots will be output to. The function will automatically recreate the file structure of the input folder with additional subfolders.</p><p><code>sampling_rate</code> refers to the sampling rate of the data. The function will assume that each signal, and each column, is using the same sampling rate.</p><p>For more information about further customizations and specifications that can be made to <code>PlotSpectrum()</code>, see <a href="./plot-signals.html">PlotSignals API</a>.</p><h3 id="plotcomparesignals" tabindex="-1"><code>PlotCompareSignals()</code> <a class="header-anchor" href="#plotcomparesignals" aria-label="Permalink to &quot;`PlotCompareSignals()`&quot;">​</a></h3><p><code>PlotCompareSignals()</code> compares plots of signals for two different stages of processing.</p><ul><li><code>in_path1</code> &lt;-- File path to input folder</li><li><code>in_path2</code> &lt;-- File path to input folder</li><li><code>out_path</code> &lt;-- File path to output folder</li><li><code>sampling_rate</code> &lt;-- Sampling rate of the data</li></ul><p><code>in_path1</code> refers to the folder containing the first group of signal data. The function will be able to detect signals in the folder, and in any subfolders.</p><p><code>in_path2</code> refers to the folder containing the second group of signal data. The function will be able to detect signals in the folder, and in any subfolders.</p><p><code>out_path</code> refers to the folder where the plots will be output to. The function will automatically recreate the file structure of the input folder with additional subfolders.</p><p><code>sampling_rate</code> refers to the sampling rate of the data. The function will assume that each signal, and each column, is using the same sampling rate.</p><p>For more information about further customizations and specifications that can be made to <code>PlotCompareSignals()</code>, see <a href="./plot-signals.html">PlotSignals API</a>.</p><hr><h2 id="sources" tabindex="-1">Sources <a class="header-anchor" href="#sources" aria-label="Permalink to &quot;Sources&quot;">​</a></h2><p>De Luca, C., Gilmore, L., Kuznetsov, M., &amp; Roy, S. (2010). Filtering the surface EMG signal: Movement artifact and baseline noise contamination. <em>Journal of Biomechanics</em>, <em>43</em>, 1.</p><p>RENSHAW, D., BICE, M. R., CASSIDY, C., ELDRIDGE, J. A., &amp; POWELL, D. W. (2010). A Comparison of Three Computer-based Methods Used to Determine EMG Signal Amplitude. <em>International Journal of Exercise Science</em>, <em>3</em>(1), 43–48.</p>',99)]))}const f=t(l,[["render",s]]);export{u as __pageData,f as default};
